/**
 * Algolia admin indexer — invoked from Payload `afterChange` hooks
 * (skill: search-engineering). Concrete implementation in Phase 4.
 */
export const ALGOLIA_INTEGRATION_VERSION = '0.0.1' as const;
