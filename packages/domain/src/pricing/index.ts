/**
 * Pricing bounded context — public surface.
 * Comparator scenario logic arrives in Phase 6 (with the comparator widget).
 */
export type ComparisonScenario = 'cheaper' | 'equal_with_benefits' | 'more_expensive';
export type ComparisonProvider = 'booking' | 'expedia' | 'hotelscom' | 'official_site';
